namespace BREPipelineFramework.Helpers
{
    /// <summary>
    /// An enumeration that contains the types of results that can be returned from an XPath expression
    /// </summary>
    public enum XPathResultTypeEnum
    {
        Value,
        Name,
        Namespace
    }
}
